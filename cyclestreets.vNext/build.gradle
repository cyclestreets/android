
dependencies {
  compile fileTree(include: ['*.jar'], dir: 'libs')
  compile project(':libraries:cyclestreets-view'),
          project(':libraries:cyclestreets-fragments'),
          'com.android.support:appcompat-v7:19.+'
}

android {
  defaultConfig {
    applicationId 'net.cyclestreets.v2'
  } // defaultConfig
} // android
