<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
	android:orientation="vertical" 
	android:layout_width="fill_parent"
	android:layout_height="fill_parent">
	<RelativeLayout 
		android:id="@+id/mapholder"
		android:layout_width="fill_parent"
		android:layout_height="0dp"
		android:orientation="vertical"
		android:layout_weight="5.0"
		android:padding="4.0dip">
	</RelativeLayout>
  <TextView
    android:id="@+id/label"
    android:gravity="center"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:text="Where was this photo taken?"/>
  <TextView
    android:id="@+id/nogeo"
    android:gravity="center"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:text="The photo could not be geolocated. To automatically locate photos, please turn on geolocation in your Camera settings."/>
	<include layout="@layout/addphotonext"/>
</LinearLayout>
